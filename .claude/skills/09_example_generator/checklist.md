# Checklist

- At least 3 examples exist, ordered from trivial to challenging.
- Every number in every worked answer has been computed and checked.
- One example is marked as the running example.
- One counterexample or edge case is included.
- Each example carries a placement tag (intuition, math, code, exercise).
- Notation matches the math section exactly.
- No example is too large to simulate mentally.
